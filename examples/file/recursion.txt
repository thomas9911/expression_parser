createList = {list, index => 
    newList = push(list, index);
    newIndex = index+1;
    if(newIndex == 15, newList, {=> createList.(newList, newIndex)})
};

// using range(15) is ofcourse better
createList.([], 0);
